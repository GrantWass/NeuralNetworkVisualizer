// Empty shim for Node.js-only modules that get imported by browser-compatible packages.
// Provides a non-null default export so Object.keys(import) doesn't throw.
const empty = {};
export default empty;
